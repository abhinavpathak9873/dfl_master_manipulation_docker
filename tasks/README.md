# Tasks

Put ordinary Python robotics experiments here. A task should show its robot,
object, backend choices, manipulation stages, and recovery branches directly.

The current canonical target is [`../example_api.txt`](../example_api.txt).
`small_tray_pick.py` will move here when the Picker 1 Gazebo adapter makes the
script runnable rather than illustrative.
